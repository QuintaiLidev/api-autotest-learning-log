🌍 Day 1：环境搭建与第一个接口测试

日期： 2025年11月10日
时长： 约 1 小时
主题： 完成开发环境搭建 + 成功运行第一个 pytest 接口自动化脚本

✅ 今日成果

已成功安装并配置：

PyCharm Community 2023.3.4

Python 3.x

pip 国内镜像源（清华源）

requests / pytest / allure-pytest

成功运行第一个接口测试脚本：

import requests
import pytest

def test_get_demo():
    url = "https://httpbin.org/get"
    response = requests.get(url)
    assert response.status_code == 200
    assert "url" in response.json()


运行结果： ✅ PASSED
（截图：建议在此插入运行结果截图）

🧩 今日关键收获

理解了 requests 库：

会通过 requests.get() 向接口发送请求；

会通过 response.json() 获取接口返回内容。

理解了 pytest：

会创建 test_ 开头的函数；

会通过 assert 进行断言。

理解了环境隔离（venv）：

每个项目独立环境管理依赖，不会冲突。

🧠 小技巧笔记
项目	作用
pip list	查看已安装的依赖包
pip install xxx -i 清华源	使用国内源快速安装
pytest -v	运行项目中所有测试用例
requests.get(url)	发起 GET 请求
assert	验证返回结果是否符合预期