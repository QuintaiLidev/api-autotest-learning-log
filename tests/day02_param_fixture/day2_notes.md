# Day 2：接口参数化 + Fixture

**日期：** 2025-11-11  
**目标：** 掌握 pytest 参数化和 fixture 的基本使用  

---

### ✅ 今日完成
- 会使用 `@pytest.mark.parametrize` 实现多组数据自动测试；
- 会定义 `fixture` 实现测试前后操作；
- 理解了 pytest 的用例执行顺序。

---

### 🧩 关键代码
```python
@pytest.mark.parametrize("endpoint, expected_status", [
    ("/get", 200),
    ("/status/200", 200),
    ("/status/404", 404)
])
